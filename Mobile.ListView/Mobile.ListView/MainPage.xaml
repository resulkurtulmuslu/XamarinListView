<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Mobile.ListView.MainPage">

    <!--<ListView x:Name="lstView">
        <ListView.ItemsSource>
            <x:Array Type="{x:Type x:String}">
                <x:String>İtem 1</x:String>
                <x:String>İtem 2</x:String>
                <x:String>İtem 3</x:String>
                <x:String>İtem 4</x:String>
                <x:String>İtem 5</x:String>
            </x:Array>
        </ListView.ItemsSource>
    </ListView>-->

    <ListView x:Name="lstView" 
              SeparatorVisibility="Default"
              SeparatorColor="Gray">
        <ListView.Header>
        <StackLayout>
            <Label x:Name="header" Text="Header" BackgroundColor="Gray" TextColor="White"></Label>
        </StackLayout>
        </ListView.Header>
        <ListView.Footer>
            <StackLayout>
                <Label x:Name="footer" Text="Footer" BackgroundColor="Gray" TextColor="White"></Label>
            </StackLayout>
        </ListView.Footer>
    </ListView>

</ContentPage>
